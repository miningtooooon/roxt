# Admin Dashboard (Web)

## Run locally
1) Copy `.env.example` to `.env` and set `VITE_API_URL`.
2) Install & start:
```bash
npm install
npm run dev
```

Login uses **secret code only** via backend `/admin/auth/login`.
