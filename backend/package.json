{
  "name": "rewards-backend-single",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "npm --prefix ../admin install && npm --prefix ../miniapp install && npm --prefix ../admin run build && npm --prefix ../miniapp run build",
    "start": "node src/index.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "helmet": "^7.1.0",
    "dotenv": "^16.4.5"
  }
}